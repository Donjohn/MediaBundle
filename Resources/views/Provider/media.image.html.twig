<img src="{{ asset(mediaPath) }}" {% if class|default(false) %}class="{{ class }}"{% endif %} {% if height|default(false) %}height="{{ height }}"{% endif %} {% if width|default(false) %}width="{{ width }}"{% endif %} {% if alt|default(false) %}alt="{{ alt }}"{% endif %} {% if id|default(false) %}id="{{ id }}"{% endif %} {% if style|default(false) %}style="{{ style }}"{% endif %}>
